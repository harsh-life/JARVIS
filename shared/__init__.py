"""Shared contracts importable by both server/ and (future) android/.

Per 16_REPOSITORY_MODULE_BOUNDARIES.md §1: `shared/schemas/` is the *only*
package both sides of Track B import. Nothing here may import from `server/`.
"""
