<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The Track B API and the authenticated device channel (docs/23 §4). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Reconnect when the network returns rather than on a blind timer. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- The channel runs in a visible foreground service while connected —
         never a hidden background service (docs/23 §4). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- So that foreground-service notification is actually shown (API 33+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".JarvisApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.Jarvis">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- docs/23 §3: the browser → app return after Google login. A
             verified App Link (autoVerify + the server's assetlinks.json), so no
             other app can claim it. The bootstrap token arrives in the URL
             fragment and is accepted only with this app's own login nonce. -->
        <activity
            android:name=".auth.LoginLinkActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Jarvis">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="${appLinkHost}"
                    android:path="/app/login" />
            </intent-filter>
        </activity>

        <service
            android:name=".channel.ChannelService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Persistent authenticated connection to the user's own JARVIS server" />
        </service>
    </application>

</manifest>
